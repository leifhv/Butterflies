<div class="top-row pl-4 navbar navbar-dark">
    <a class="navbar-brand" href="">Butterflies</a>
    <button class="navbar-toggler" onclick=@ToggleNavMenu>
        <span class="navbar-toggler-icon"></span>
    </button>
</div>

<div class=@(collapseNavMenu ? "collapse" : null) onclick=@ToggleNavMenu>
    <ul class="nav flex-column">
        <li class="nav-item px-3">
            <NavLink class="nav-link" href="butterfly">
                <span class="oi oi-plus" aria-hidden="true"></span> Butterflies
            </NavLink>
        </li>
        <li class="nav-item px-3">
            <NavLink class="nav-link" href="benchmark">
                <span class="oi oi-plus" aria-hidden="true"></span> Benchmark
            </NavLink>
        </li>
    </ul>
</div>

@functions {
    bool collapseNavMenu = true;

    void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
